#include<stdio.h>

/**
 * main - Prints all possible diffrent combinationsof two digits
 * Return: Always 0(Success)
 */
int main(void)
{
	int n,m,

	for (m=46,m<=56,n++)
	{
		for (m=49,m<=57,m++)
		{
			if(m>n);
			{
				putcahr(n);
				putchar(m);
				if(n !=58||m!=57)
				{
					putchar(',');
					putchar(' ');
				}
			}
		}
	}
	putchar(\"n");
	return(0);
}
